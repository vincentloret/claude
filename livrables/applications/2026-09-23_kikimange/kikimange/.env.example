# Base de données. En local : fichier SQLite. En production : Turso (dashboard turso.tech, bouton "Connect").
DATABASE_URL="file:./dev.db"
DATABASE_AUTH_TOKEN=""

# Code famille, demandé une fois par appareil sur /qui-es-tu (cookie valable 1 an ensuite).
# Si vide, l'accès est refusé.
FAMILLE_CODE=""

# Notifications push (clés VAPID, générées avec : npx web-push generate-vapid-keys)
NEXT_PUBLIC_VAPID_PUBLIC_KEY=""
VAPID_PRIVATE_KEY=""
VAPID_SUBJECT="mailto:ton-adresse@exemple.fr"

# Secret partagé entre la fonction planifiée Netlify et la route /api/rappel-lundi
CRON_SECRET=""
